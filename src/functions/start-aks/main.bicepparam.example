using 'main.bicep'

param projectName = 'startaks'
param environment = 'prod'
param location = 'eastasia'

param aksTargets = [
  {
    subscriptionId: '00000000-0000-0000-0000-000000000000'
    resourceGroup: 'rg-ghrunner-prod'
    name: 'aks-ghrunner-prod'
  }
  // 可新增更多叢集
  // {
  //   subscriptionId: '00000000-0000-0000-0000-000000000000'
  //   resourceGroup: 'rg-devops-agent-prod'
  //   name: 'aks-devops-agent-prod'
  // }
]

param tags = {
  project: 'start-aks'
  environment: 'prod'
  managedBy: 'bicep'
}

// 排程與時區採用預設值即可 (可省略，僅示範明確覆寫方式)
param timeZone = 'Asia/Taipei'
param startSchedule = '0 25,40 0 * * *'
param startFallbackSchedule = '0 0 6 * * *'
param stopSchedule = '0 0 20 * * *'

// 停止排程預設停用，確保部署後不會誤停正在使用中的叢集；
// 確認排程時段安全無虞後，再手動改為 true 啟用
param enableStopSchedule = false
